# OpenAPI

Canonical OpenAPI source:

```text
https://api.postoria.io/v1/openapi.json
```

This MCP server currently uses a small handwritten client because the Postoria Public API v1 surface is compact. If the API grows, generate a typed client here and keep MCP tools as stable agent-friendly wrappers instead of exposing raw endpoint names.
